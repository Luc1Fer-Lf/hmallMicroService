server:
  port: 8080
spring:
  application:
    name: gateway # 服务名
  profiles:
    active: dev
  cloud:
      nacos:
        server-addr: 47.93.37.97:8848
        config:
          file-extension: yaml
          shared-configs:
            - data-id: shared-log.yaml